# Development and Training Dependencies
# Use this file for local development and model training
# Install with: pip install -r requirements-dev.txt

# Runtime dependencies (also in requirements.txt)
Flask==3.0.0
numpy==1.26.2
scikit-learn==1.3.2
Werkzeug==3.0.3

# Additional dependencies for development and training only
pandas==2.1.3
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: Development server (not needed on Vercel)
gunicorn==22.0.0
